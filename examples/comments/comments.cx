// comments.cx — M1 样例：行注释与块注释
// 这是行注释：从 // 到行尾被忽略

/* 这是块注释：
   可以跨越多行 */

/* 块注释不嵌套（与 C 语言一致）：
   块注释内遇到第一个“星号+斜杠”即结束，
   之后的内容按普通代码继续词法分析 */

func main(): i32 {
    /* 函数体中的块注释 */
    var x: i32 = 1; // 行尾注释
    printf("%d\n", x);
    return 0;
}
